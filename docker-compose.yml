version: '3.8'

services:
  appliance-health-monitor:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: appliance_health_monitor
    ports:
      - "8000:8000"
    volumes:
      - .:/app
    environment:
      - PYTHONUNBUFFERED=1
    restart: unless-stopped
